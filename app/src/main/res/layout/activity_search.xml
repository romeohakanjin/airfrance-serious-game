<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:background="@drawable/login_gradient"
    tools:context=".SearchActivity">

    <TextView
        android:id="@+id/choiceSearchTextView"
        android:layout_width="wrap_content"
        android:layout_height="19dp"
        android:text="Recherche"
        android:textColor="@color/colorWhite"
        tools:layout_editor_absoluteX="84dp"
        tools:layout_editor_absoluteY="85dp" />

    <Spinner
        android:id="@+id/choiceSearchSpinner"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textAlignment="center"
        tools:text="Choix de la recherche"/>

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="number"
        android:textColor="@color/colorWhite"
        android:id="@+id/number_search_edittext"
        android:hint="Num de la recherche"/>

    <Button
        android:id="@+id/searchValidateButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/button_validate"
        android:textColor="@color/colorWhite"
        android:background="@drawable/round_button"
        tools:layout_editor_absoluteX="148dp"
        tools:layout_editor_absoluteY="239dp" />
</LinearLayout>